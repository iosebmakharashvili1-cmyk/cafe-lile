{
  "name": "cafe-lile-ordering",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev:api": "pnpm --filter ordering-api dev",
    "dev:customer": "pnpm --filter customer-web dev",
    "dev:admin": "pnpm --filter admin-web dev",
    "build:customer": "pnpm --filter customer-web build",
    "build:admin": "pnpm --filter admin-web build",
    "deploy:api": "pnpm --filter ordering-api deploy",
    "migrate:local": "pnpm --filter ordering-api migrate:local",
    "migrate:staging": "pnpm --filter ordering-api migrate:staging",
    "migrate:prod": "pnpm --filter ordering-api migrate:prod"
  }
}
