{
  "name": "ordering-api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate:local": "wrangler d1 migrations apply ordering-db --local",
    "migrate:staging": "wrangler d1 migrations apply ordering-db --env staging --remote",
    "migrate:prod": "wrangler d1 migrations apply ordering-db --env production --remote"
  },
  "dependencies": {
    "@cafe-lile/contracts": "workspace:*",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "wrangler": "^4.0.0",
    "typescript": "^5.5.4",
    "@cloudflare/workers-types": "^4.20240925.0"
  }
}
